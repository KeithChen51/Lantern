# Content Source Registry

This directory stores confirmed content sources before they are transformed into module data, admin records, or published pages.

## Action / 笃行

- `action-canwu-cases/2026-06-final/` stores the finalized "一般参悟案例-格式统一版" batch.
- Status: final confirmed source material, staged for future import into `Action / 笃行`.
- Current project meaning: the source is known and traceable, but it is not yet published as structured `ActionCase` data or managed content.
- Integration rule: keep the original source Markdown unchanged; convert or copy into structured content when the Action import work starts.
